# Issue: 权限初始化与菜单配置

Status: done

## Parent

[03-delete-status-export](issues/03-delete-status-export.md)

## What to build

初始化料号管理的权限数据，配置前端路由权限要求，添加侧边栏菜单项，并为所有功能按钮添加权限指令控制。确保权限体系完整，无权限用户看不到对应的菜单和按钮。

## Acceptance criteria

- [x] 数据库 `sys_permission` 表中存在 5 条料号权限记录：material:read、material:add、material:edit、material:delete、material:export
- [x] 前端路由 `/materials` 配置了 `meta.permission: 'material:read'`
- [x] 前端侧边栏"基础数据"分组下显示"料号管理"菜单项
- [x] 前端菜单项配置了 `permission: 'material:read'`
- [x] 前端菜单项图标使用 `Box`（Element Plus 图标）
- [x] 前端新增按钮添加了 `v-permission="'material:add'"` 指令
- [x] 前端编辑按钮添加了 `v-permission="'material:edit'"` 指令
- [x] 前端删除按钮添加了 `v-permission="'material:delete'"` 指令
- [x] 前端批量删除按钮添加了 `v-permission="'material:delete'"` 指令
- [x] 前端导出按钮添加了 `v-permission="'material:export'"` 指令
- [x] 无 material:read 权限的用户不显示"料号管理"菜单项
- [x] 无 material:add 权限的用户看不到新增按钮
- [x] 无 material:delete 权限的用户看不到删除和批量删除按钮
- [x] 无 material:export 权限的用户看不到导出按钮
- [x] 无 material:read 权限的用户访问 `/materials` 跳转到 403 页面

## Blocked by

- [03-delete-status-export](issues/03-delete-status-export.md) - 需要功能实现完成

## Technical Details

### 权限初始化数据

在数据库 `sys_permission` 表插入以下记录：

| permission_name | permission_code |
|-----------------|-----------------|
| 查看料号 | material:read |
| 新增料号 | material:add |
| 编辑料号 | material:edit |
| 删除料号 | material:delete |
| 导出料号 | material:export |

权限采用扁平化命名 `{模块}:{操作}`，符合 CONTEXT.md 定义。

可通过 SQL 初始化脚本或后端初始化逻辑执行。

### 前端路由配置

在 `mattmes-ui/src/router/index.ts` 添加路由：

```typescript
{
  path: '/materials',
  name: 'Materials',
  component: () => import('@/views/material/MaterialList.vue'),
  meta: {
    title: '料号管理',
    permission: 'material:read'
  }
}
```

### 前端菜单配置

在 `mattmes-ui/src/router/menus.ts` 的"基础数据"分组下添加菜单项：

```typescript
{
  path: '/materials',
  title: '料号管理',
  icon: 'Box',
  permission: 'material:read'
}
```

### 权限指令使用

在按钮上使用 `v-permission` 指令：
- `v-permission="'material:add'"` - 新增按钮
- `v-permission="'material:edit'"` - 编辑按钮、状态开关
- `v-permission="'material:delete'"` - 删除按钮、批量删除按钮
- `v-permission="'material:export'"` - 导出按钮

### 参考先例

- 权限数据：参考 `sys_permission` 表现有数据结构
- 路由配置：参考 `/projects` 路由配置
- 菜单配置：参考 `menus.ts` 中项目管理菜单项
- 权限指令：参考 `ProjectList.vue` 中按钮权限指令

---

## Completion Notes

**2026-06-08 完成记录:**

- 状态: `ready-for-agent` → `done`
- 实现方式: 直接实现（数据库 SQL + 前端配置修改）

**数据库权限初始化:**
- 插入模块权限: id=23, permission_code='material'
- 插入操作权限: id=24-28 (material:read, material:add, material:edit, material:delete, material:export)

**前端配置修改:**
1. `src/router/index.ts` - 路由 meta 添加 `permission: 'material:read'`
2. `src/router/menus.ts` - 菜单项添加 `permission: 'material:read'`

**按钮权限指令（已在 Issue 03 中实现）:**
- 新增按钮: `v-permission="'material:add'"`
- 编辑按钮: `v-permission="'material:edit'"`
- 状态开关: `v-permission="'material:edit'"`
- 删除按钮: `v-permission="'material:delete'"`
- 批量删除按钮: `v-permission="'material:delete'"`
- 导出按钮: `v-permission="'material:export'"`

**验证结果:**
- 数据库权限数据正确插入（6条记录）
- 前端项目编译成功

---

## Comments

> *This was generated by AI during triage.*
>
> ## Agent Brief
>
> **任务摘要**: 初始化料号管理模块的权限数据，配置前端路由权限要求，添加侧边栏菜单项，并为所有功能按钮添加权限指令控制。
>
> **实现步骤**:
>
> ### 1. 数据库权限初始化
>
> 在 `sys_permission` 表插入以下记录（参考项目权限 id:17-22 的结构）：
>
> ```sql
> -- 先插入模块级权限
> INSERT INTO sys_permission (permission_name, permission_code, permission_type, parent_id, sort_order, description, created_by, create_time, updated_by, update_time)
> VALUES ('料号管理', 'material', 1, 0, 6, '料号管理模块', 'system', NOW(), 'system', NOW());
>
> -- 获取刚插入的模块ID（假设为 @material_id）
> -- 再插入操作级权限
> INSERT INTO sys_permission (permission_name, permission_code, permission_type, parent_id, sort_order, description, created_by, create_time, updated_by, update_time)
> VALUES 
>   ('料号查看', 'material:read', 2, @material_id, 1, '查看料号列表', 'system', NOW(), 'system', NOW()),
>   ('料号新增', 'material:add', 2, @material_id, 2, '新增料号', 'system', NOW(), 'system', NOW()),
>   ('料号编辑', 'material:edit', 2, @material_id, 3, '编辑料号', 'system', NOW(), 'system', NOW()),
>   ('料号删除', 'material:delete', 2, @material_id, 4, '删除料号', 'system', NOW(), 'system', NOW()),
>   ('料号导出', 'material:export', 2, @material_id, 5, '导出料号数据', 'system', NOW(), 'system', NOW());
> ```
>
> ### 2. 前端路由配置
>
> 文件: `mattmes-ui/src/router/index.ts`
>
> 在现有路由数组中添加（参考 `/projects` 路由配置）：
>
> ```typescript
> {
>   path: '/materials',
>   name: 'Materials',
>   component: () => import('@/views/material/MaterialList.vue'),
>   meta: {
>     title: '料号管理',
>     permission: 'material:read'
>   }
> }
> ```
>
> ### 3. 前端菜单配置
>
> 文件: `mattmes-ui/src/router/menus.ts`
>
> 1. 导入 Box 图标：
> ```typescript
> import { ..., Box } from '@element-plus/icons-vue'
> ```
>
> 2. 在菜单数组中添加（建议放在项目管理后面）：
> ```typescript
> {
>   path: '/materials',
>   title: '料号管理',
>   icon: Box,
>   permission: 'material:read'
> }
> ```
>
> ### 4. 前端按钮权限指令
>
> 文件: `mattmes-ui/src/views/material/MaterialList.vue`
>
> 在现有按钮上添加 `v-permission` 指令（参考 `ProjectList.vue` 实现）：
>
> - 新增按钮: `v-permission="'material:add'"`
> - 编辑按钮: `v-permission="'material:edit'"`
> - 状态开关: `v-permission="'material:edit'"`
> - 删除按钮: `v-permission="'material:delete'"`
> - 批量删除按钮: `v-permission="'material:delete'"`
> - 导出按钮: `v-permission="'material:export'"`
>
> **验证方式**:
>
> 1. 数据库验证：查询 `sys_permission` 表确认 6 条记录（1 模块 + 5 操作）
> 2. 编译验证：前后端均能正常编译
> 3. 功能验证：
>    - 使用 admin 账户登录，确认能看到料号管理菜单
>    - 创建一个无 `material:read` 权限的测试角色，确认用户看不到料号管理菜单
>    - 无 `material:add` 权限的用户看不到新增按钮
>
> **参考文件**:
> - 数据库结构: `sys_permission` 表现有数据
> - 路由配置: `mattmes-ui/src/router/index.ts:43-61`
> - 菜单配置: `mattmes-ui/src/router/menus.ts`
> - 权限指令: `mattmes-ui/src/views/project/ProjectList.vue:45-101`