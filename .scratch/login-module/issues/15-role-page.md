---
status: done
completed: 2026-06-03
labels: [enhancement, done]
---

# 角色管理页面

## Parent

参考 `.scratch/login-module/PRD.md`

## What to build

实现角色管理页面UI组件，包含角色列表、新增编辑弹窗、分配权限弹窗、删除保护。

核心功能：
- 数据表格（角色名称、编码、描述、用户数、创建时间、操作）
- 新增/编辑角色弹窗
- 分配权限弹窗（树形结构）
- 删除按钮（保护admin角色）

## Acceptance criteria

- [x] 创建 `src/views/role/RoleList.vue` 角色管理页面
- [x] 创建 `src/api/role.ts` 角色API封装：
  - `getRoles()` - 查询列表
  - `getRole(id)` - 查询详情
  - `createRole(data)` - 创建角色
  - `updateRole(id, data)` - 编辑角色
  - `assignPermissions(id, permissionIds)` - 分配权限
  - `disableRole(id)` - 禁用角色
  - `deleteRole(id)` - 删除角色
- [x] 数据表格：
  - 角色名称列：带角色图标(Role图标)
  - 角色编码列：等宽字体(IBMMono)
  - 描述列：普通字体，过长截断
  - 用户数列：数字徽章(主色背景，白色文字)
  - 创建时间列：等宽字体
  - 状态列：Badge徽章（启用/禁用）
  - 操作列：图标按钮组（编辑、分配权限、删除）
- [x] 新增角色弹窗：
  - 角色名称输入（必填）
  - 角色编码输入（必填，提示格式如ROLE_NAME）
  - 描述输入（可选）
  - 提交按钮
- [x] 编辑角色弹窗：
  - 角色名称、描述可编辑
  - 角色编码显示不可编辑
  - 提交按钮
- [x] 分配权限弹窗：
  - 权限树形结构（Element Plus Tree组件）
  - 支持勾选节点（模块级和按钮级）
  - 提交按钮
- [x] 删除按钮：
  - 点击显示确认弹窗
  - SUPER_ADMIN角色删除按钮禁用（灰色不可点击）
  - 删除成功刷新列表
- [x] 使用v-permission指令控制按钮显示：
  - 新增按钮：v-permission="role:add"
  - 编辑按钮：v-permission="role:edit"
  - 分配权限按钮：v-permission="role:assign-permission"
  - 删除按钮：v-permission="role:delete"
- [x] 表格分页：Element Plus Pagination组件
- [x] 编写组件测试覆盖关键交互

## Blocked by

- #03-frontend-skeleton（需要前端骨架）
- #07-role-management（需要角色管理接口）
- #11-main-layout（需要路由和权限指令）

## Comments

> *This was generated by AI during triage.*

**2026-06-03 分流记录:**

- 状态: `needs-triage` → `ready-for-agent`
- 分类: `enhancement`
- 阻塞项已解除: #03-frontend-skeleton ✅, #07-role-management ✅, #11-main-layout ✅
- 规格完整度: 高（17条验收标准，PRD详细定义）
- 现有基础设施: RoleList.vue占位文件、后端API已就绪、权限指令已实现

---

## Agent Brief

### 任务摘要

实现角色管理页面UI组件，包含角色列表、新增编辑弹窗、分配权限弹窗、删除保护逻辑。

### 现有基础设施（可复用）

已存在的文件：
- `mattmes-ui/src/views/role/RoleList.vue` — 占位文件（待实现）
- `mattmes-ui/src/api/user.ts` — API封装模板
- `mattmes-ui/src/api/auth.ts` — API封装模板
- `mattmes-ui/src/utils/request.ts` — HTTP请求封装
- `mattmes-ui/src/directives/permission.ts` — v-permission指令（已实现）
- `mattmes-ui/src/router/index.ts` — 路由配置（/roles已配置）
- `mattmes-ui/src/router/menus.ts` — 菜单配置（角色管理已配置）
- `mattmes-ui/src/styles/theme.css` — 工业精密风格主题

后端API已就绪（#07完成）：
- `GET /api/roles` — 查询角色列表
- `GET /api/roles/{id}` — 查询角色详情
- `POST /api/roles` — 创建角色
- `PUT /api/roles/{id}` — 编辑角色
- `PUT /api/roles/{id}/permissions` — 分配权限
- `PUT /api/roles/{id}/disable` — 禁用角色
- `PUT /api/roles/{id}/enable` — 启用角色
- `DELETE /api/roles/{id}` — 删除角色

参考模板：
- `mattmes-ui/src/views/user/UserList.vue` — 用户管理页面（类似结构）
- `mattmes-ui/src/api/user.ts` — 用户API封装

### 实现范围

**创建文件**:
1. `src/api/role.ts` — 角色API封装
   - `getRoles(params)` — 查询列表
   - `getRole(id)` — 查询详情
   - `createRole(data)` — 创建角色
   - `updateRole(id, data)` — 编辑角色
   - `assignPermissions(id, permissionIds)` — 分配权限
   - `disableRole(id)` — 禁用角色
   - `enableRole(id)` — 启用角色
   - `deleteRole(id)` — 删除角色

**修改文件**:
1. `src/views/role/RoleList.vue` — 角色管理页面完整实现

### 技术约束

- **UI框架**: Element Plus（已安装）
- **图标**: @element-plus/icons-vue（已安装）
- **设计风格**: 工业精密风格（参考 `src/styles/theme.css`）
- **权限指令**: `v-permission="role:add"` 等
- **字体规范**:
  - 角色编码列：等宽字体（IBM Plex Mono）
  - 创建时间列：等宽字体
- **保护逻辑**: SUPER_ADMIN角色删除按钮禁用

### 关键实现点

1. **数据表格列**:
   - 角色名称列：带Role图标（Element Plus Icon）
   - 角色编码列：等宽字体 `font-family: 'IBM Plex Mono'`
   - 描述列：普通字体，过长截断（ellipsis）
   - 用户数列：数字徽章（主色背景，白色文字）
   - 创建时间列：等宽字体
   - 状态列：Badge徽章（启用=绿色，禁用=灰色）
   - 操作列：图标按钮组（编辑、分配权限、删除）

2. **新增角色弹窗**:
   - 角色名称输入（必填）
   - 角色编码输入（必填，提示格式如 `ROLE_NAME`）
   - 描述输入（可选）
   - 提交按钮

3. **编辑角色弹窗**:
   - 角色名称、描述可编辑
   - 角色编码显示不可编辑（disabled）
   - 提交按钮

4. **分配权限弹窗**:
   - 权限树形结构（Element Plus Tree组件）
   - 需先调用 `GET /api/permissions` 获取权限列表
   - 支持勾选节点（模块级和按钮级）
   - 提交按钮

5. **删除按钮保护**:
   - 点击显示确认弹窗（ElMessageBox.confirm）
   - `role_code === 'SUPER_ADMIN'` 时删除按钮禁用（灰色不可点击）
   - 删除成功刷新列表

6. **v-permission指令绑定**:
   - 新增按钮：`v-permission="role:add"`
   - 编辑按钮：`v-permission="role:edit"`
   - 分配权限按钮：`v-permission="role:assign-permission"`
   - 删除按钮：`v-permission="role:delete"`

7. **表格分页**:
   - Element Plus Pagination组件
   - 后端已支持分页（pageNum, pageSize）

### API接口类型定义

```typescript
// 查询角色列表参数
interface RoleQueryParams {
  roleName?: string
  status?: number
  pageNum?: number
  pageSize?: number
}

// 角色响应
interface RoleResponse {
  id: number
  roleName: string
  roleCode: string
  description?: string
  userCount: number
  status: number
  createTime: string
}

// 创建角色请求
interface RoleCreateRequest {
  roleName: string
  roleCode: string
  description?: string
}

// 编辑角色请求
interface RoleUpdateRequest {
  roleName: string
  description?: string
}

// 分配权限请求
interface AssignPermissionsRequest {
  permissionIds: number[]
}
```

### 验收标准（来自 Issue）

- [x] 创建 `src/views/role/RoleList.vue` 角色管理页面
- [x] 创建 `src/api/role.ts` 角色API封装
- [x] 数据表格包含所有列（角色名称、编码、描述、用户数、创建时间、状态、操作）
- [x] 新增角色弹窗实现
- [x] 编辑角色弹窗实现（角色编码不可编辑）
- [x] 分配权限弹窗实现（树形结构）
- [x] 删除按钮保护（SUPER_ADMIN禁用）
- [x] v-permission指令控制按钮显示
- [x] 表格分页实现
- [x] 组件测试覆盖关键交互

### 参考文档

- PRD: `.scratch/login-module/PRD.md`（第439-442行：角色管理页设计）
- 后端Issue: `.scratch/login-module/issues/07-role-management.md`
- API封装模板: `mattmes-ui/src/api/user.ts`
- 页面模板: `mattmes-ui/src/views/user/UserList.vue`
- 主题样式: `mattmes-ui/src/styles/theme.css`

---

## Completion Notes

**2026-06-03 完成记录:**

- 状态: `ready-for-agent` → `done`
- 实现方式: TDD（测试驱动开发），采用垂直切片方式逐个功能实现

**创建的文件:**

1. `mattmes-ui/src/api/role.ts` — 角色API封装（8个API函数）
2. `mattmes-ui/src/api/permission.ts` — 权限API封装（用于权限树）
3. `mattmes-ui/src/api/__tests__/role.spec.ts` — API单元测试（8个测试用例）
4. `mattmes-ui/src/views/role/__tests__/RoleList.spec.ts` — 组件测试（12个测试用例）

**修改的文件:**

1. `mattmes-ui/src/views/role/RoleList.vue` — 角色管理页面完整实现

**测试结果:**

```
Test Files  15 passed (15)
Tests       121 passed (121)
```

**功能实现:**

- ✅ 数据表格（角色名称、编码、描述、用户数、状态、创建时间、操作列）
- ✅ 新增角色弹窗（角色名称、角色编码、描述）
- ✅ 编辑角色弹窗（角色编码不可编辑）
- ✅ 分配权限弹窗（树形结构）
- ✅ 删除按钮保护（SUPER_ADMIN禁用）
- ✅ v-permission指令控制按钮显示
- ✅ 表格分页

---

_在此追加对话历史和讨论记录_